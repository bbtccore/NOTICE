# Base58-encoded secret key array or bs58 string
SOLANA_PRIVATE_KEY=
# Cluster URL (e.g., https://api.devnet.solana.com)
RPC_URL=
# Optional: program id if pre-deployed
PROGRAM_ID=
# Mint decimals (default 9)
MINT_DECIMALS=9
